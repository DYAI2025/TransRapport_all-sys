# TransRapport Offline Requirements
# Fixed versions for offline installation
# Install with: pip install --no-index --find-links=bundle/wheels -r requirements-offline.txt

# Core dependencies
click==8.1.6
pyyaml==6.0.1
regex
sqlcipher3-binary
matplotlib
pandas
numpy

# Audio processing
pyaudio; platform_system=='Windows'
sounddevice
librosa
praat-parselmouth
opensmile

# ML/AI dependencies
torch==2.3.0
torchaudio==2.3.0
openai-whisper
whisperx
pyannote.audio

# Optional UI (removed chart.js and date-fns - they are JavaScript libraries, not Python)
# chart.js  # REMOVED - JavaScript library
# date-fns  # REMOVED - JavaScript library

# Development (optional)
pytest>=7.0.0
ruff>=0.1.0
black>=23.0.0

# Legacy dependencies (from original file)
scipy>=1.10.0
pyannote.core>=5.0.0
pyannote.pipeline>=3.0.0
soundfile>=0.12.0
pydub>=0.25.0
webrtcvad>=2.0.0
spacy>=3.7.0
transformers>=4.30.0
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
reportlab>=4.0.0
jinja2>=3.1.0
markdown>=3.5.0
pytest-asyncio>=0.21.0